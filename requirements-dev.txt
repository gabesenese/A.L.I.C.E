-r requirements.txt
-r requirements-api.txt

pytest>=8.0
pytest-asyncio>=0.23
pytest-cov>=4.1
httpx>=0.27
# Exact pins: both tools change what they flag between releases, so a floor
# lets a new release turn CI red on code nobody touched. Keep ruff in step with
# the rev in .pre-commit-config.yaml, or the hook and CI disagree on format.
ruff==0.16.8
pyright==1.1.414
pre-commit>=3.7
