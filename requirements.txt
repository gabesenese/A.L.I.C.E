# A.L.I.C.E default runtime dependencies (lean path)
# Keep this file focused on the day-to-day companion runtime.

# Core runtime + NLP routing
numpy>=1.21.0
scikit-learn>=1.0.0
nltk>=3.6.0
sentence-transformers>=2.2.0
requests>=2.31.0

# Runtime quality and UX
psutil>=5.8.0
rich>=13.0.0
python-json-logger>=2.0.0
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0
pydantic-settings>=2.2.1
httpx>=0.27.0
structlog>=24.2.0
chromadb>=0.5.0
opentelemetry-sdk>=1.25.0
opentelemetry-exporter-otlp>=1.25.0

# Optional bundles (install only what you use)
#   pip install -r requirements-dev.txt
#   pip install -r requirements-voice.txt
#   pip install -r requirements-integrations.txt
#   pip install -r requirements-ops.txt
